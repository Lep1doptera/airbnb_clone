<!DOCTYPE html>
<html>
  <head>
    <title>AirbnbClone</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
  </head>

  <body>
    <div data-controller="flash-messages" data-flash="<%= JSON.dump(flash.to_a) %>"></div>

      <nav class="navbar navbar-expand-lg navbar-light bg-primary px-4 d-flex justify-content-between align-items-center position-relative">
        <%= link_to 'Properties', properties_path, class: 'btn btn-dark' %>
        <a class="navbar-brand fs-2 position-absolute top-50 start-50 translate-middle" href="<%= root_path %>">Airbnb</a>

        <div class="ms-auto">
          <% if user_signed_in? %>
            <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn btn-dark' %>
          <% else %>
            <%= link_to 'Login', new_user_session_path, class: 'btn btn-outline-dark' %>
          <% end %>
        </div>
    </nav>

    <%= yield %>
  </body>
  <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
</html>
